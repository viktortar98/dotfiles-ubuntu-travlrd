# Machine Secret Handling

This repository does not manage project-level secrets.

Manage only machine-level secrets manually on each device:

- SSH private keys
- Signing keys and keystores
- Auth tokens and credential caches

Do not commit those files. See `.gitignore` for protected patterns.
